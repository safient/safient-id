import React, { useState } from 'react';
import Layout from '../components/Layouts/Layout';

const Home = (props) => {
  return (
    <Layout>
      <h1>Profile page</h1>
    </Layout>
  );
};

export default Home;
